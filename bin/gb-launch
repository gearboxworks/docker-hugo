#!/bin/bash

CONTAINER="$(basename $0)"
DIR="$(dirname $0)"
EXEC="${DIR}/gb-launch-$(uname -s)"
if [ ! -x "${EXEC}" ]
then
	echo "Gearbox: Architecture not supported, ${EXEC}."
	exit
fi

exec "${EXEC}" -gb-name ${CONTAINER} "$@"
